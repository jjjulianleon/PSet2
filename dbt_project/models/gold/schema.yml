version: 2

models:
  - name: dim_date
    columns:
      - name: date_key
        tests:
          - unique
          - not_null

  - name: dim_vendor
    columns:
      - name: vendor_key
        tests:
          - unique
          - not_null
