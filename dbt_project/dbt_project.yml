name: nyc_tlc
version: '1.0.0'

profile: nyc_tlc

model-paths: ["models"]
test-paths: ["tests"]

models:
  nyc_tlc:
    silver:
      +materialized: view
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
